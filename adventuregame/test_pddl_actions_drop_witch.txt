(:action DROP
    :parameters (?e - takeable ?p - player ?i - inventory ?r - room ?f - floor)
    :precondition (and
        (at ?p ?r)
        (at ?e ?r)
        (in ?e ?i)
        (at ?f ?r)
        )
    :effect (and
        (not (in ?e ?i))
        (on ?e ?f)
        )
)