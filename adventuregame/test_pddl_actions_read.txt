(:action EXAMINE
    :parameters (?p - player ?r - room ?e - entity)
    :precondition (and
        (at ?p ?r)
        (at ?e ?r)
        (accessible ?e)
        (readable ?e)
        )
    :effect (and
    )
)